# TikTok Comment Exporter — Chrome 扩展设计方案 (v2 - 研究优化版)

> 日期: 2026-02-27
> 版本: v2（基于三轮搜索研究和页面实际分析优化）
> 状态: 已确认，待实施

## 概述

Chrome 扩展插件，用于导出 TikTok 视频页面的所有评论数据。通过拦截 TikTok 评论 API 响应自动采集，支持 CSV 导出和纯文本复制。

## 三轮搜索研究总结

### 第一轮：核心方案调研
1. **Fetch 注入拦截模式**（dev.to 最佳实践）：页面注入脚本 → 覆写 fetch → CustomEvent → Content Script → Background。已被多个生产级扩展验证。
2. **Zeeschuimer**（开源研究工具）：使用 WebRequest API 被动拦截网络流量，支持 TikTok 评论。但 Chrome MV3 下无法读取响应体。
3. **ExportComments.com**（商业 SaaS）：成熟的评论导出服务，支持 20+ 平台。

### 第二轮：技术深入
1. **MV3 限制确认**：`chrome.webRequest` 在 MV3 中无法读取响应体，必须用页面脚本注入方式。
2. **Reddit 社区验证**：CSP 限制下，必须使用 `web_accessible_resources` 声明注入脚本。
3. **TikTok 页面实际分析**：发现双层 Tab 系统（旧 `data-e2e` + 新 `TUXTabBar`），滚动容器为 `DivCommentMain`。

### 第三轮：框架与标准
1. **WXT > Plasmo > Vanilla**：WXT 是 2025/2026 最活跃的扩展框架，但对单功能插件来说 Vanilla JS 更简单。
2. **Service Worker 关键限制**：空闲 30 秒后被终止，所有内存变量丢失。必须用 `chrome.storage.session` 持久化。
3. **chrome.storage.session** 10MB 限制，足够存储数千条评论。

### 最终方案选择
- **Vanilla JavaScript + Manifest V3**：项目简单，无需框架开销
- **Fetch 注入 + CustomEvent 通信**：最可靠的 API 响应拦截方式
- **chrome.storage.session**：Service Worker 安全的状态管理

## 原设计缺陷与修复

| 原设计问题 | 修复方案 |
|-----------|---------|
| 使用 `postMessage` 通信 | 改用 `CustomEvent`，避免与页面自身消息冲突 |
| Background 数据存内存 | 改用 `chrome.storage.session`，Service Worker 重启安全 |
| 未考虑 TUXTabBar 双层 Tab | Content Script 需检测并适配新旧 Tab 系统 |
| 滚动目标为 `DivCommentListContainer` | 实际滚动容器为 `DivCommentMain`（overflowY: scroll） |
| 未处理 Service Worker 终止 | 添加 `chrome.runtime.onStartup` 状态恢复 |
| 未考虑 CSP 限制 | 使用 `web_accessible_resources` 声明拦截器脚本 |

## 关键决策（不变）

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 数据范围 | 顶级评论 + 所有回复 | 数据完整性 |
| 导出格式 | CSV | Excel/Sheets 可直接打开 |
| 复制格式 | 纯文本（带缩进回复） | 粘贴到任何地方可读 |
| 采集方式 | API 拦截（fetch 注入） | 数据精确，不受 DOM 变化影响 |
| 加载方式 | 自动滚动 | 用户体验最佳 |
| 导出字段 | 核心 9 字段 | 覆盖大多数分析场景 |
| UI 形式 | Popup 弹窗 | 实现简单，符合扩展标准交互 |

## 架构（优化版）

```
┌─────────────┐  chrome.runtime   ┌──────────────┐  chrome.runtime  ┌───────────────┐  CustomEvent  ┌──────────────┐
│  Popup UI   │ ◄──────────────► │  Background   │ ◄─────────────► │ Content Script │ ◄──────────► │ Interceptor  │
│ (popup/)    │   .sendMessage   │ (Service      │  .sendMessage   │ (content.js)   │  (window)    │ (injected)   │
│             │                  │  Worker)      │                 │                │              │              │
│ - 采集按钮   │                  │ - 状态持久化   │                 │ - 注入拦截器    │              │ - 覆写 fetch │
│ - 导出CSV   │                  │   storage.    │                 │ - 自动滚动      │              │ - 匹配 URL   │
│ - 复制按钮   │                  │   session     │                 │ - 转发数据      │              │ - 克隆响应    │
│ - 进度显示   │                  │ - 数据去重     │                 │ - 页面检测      │              │ - 派发事件    │
└─────────────┘                  └──────────────┘                 └───────────────┘              └──────────────┘
```

## 数据流（优化版）

```
1. 用户打开 TikTok 视频页
   → Content Script 自动注入（document_start）
   → 注入 interceptor.js 到页面上下文（MAIN world）
   → 拦截器开始监听 fetch 请求

2. 用户点击 "采集评论"
   → Popup → chrome.runtime.sendMessage → Background
   → Background → chrome.tabs.sendMessage → Content Script
   → Content Script 开始自动滚动评论区

3. TikTok 加载更多评论
   → 拦截器捕获 /api/comment/list/ 响应
   → resp.clone().json() 提取数据
   → window.dispatchEvent(new CustomEvent('tce_api_data', { detail }))
   → Content Script 监听 CustomEvent
   → chrome.runtime.sendMessage → Background

4. Background 处理数据
   → 按 cid 去重
   → 存入 chrome.storage.session
   → 通知 Popup 更新进度

5. 采集完成（has_more === 0 或超时）
   → 导出/复制按钮激活

6. 用户点击导出/复制
   → Popup 从 Background 获取数据
   → 生成 CSV 下载 / 格式化文本到剪贴板
```

## TikTok 页面实际 DOM 结构（2026-02 实测）

```
页面结构关键节点:
├── TUXTabBar (新版 Tab 系统)
│   ├── TUXTabBar-item: "Comments" (需点击切换)
│   └── TUXTabBar-item: "You may like"
│
├── DivCommentMain (overflowY: scroll) ← 实际滚动容器
│   ├── DivCommentCountContainer → "560 comments"
│   └── DivCommentListContainer (overflow: visible)
│       └── DivCommentObjectWrapper × N
│           └── DivCommentItemWrapper
│               ├── [data-e2e="comment-username-1"] → 用户名
│               ├── [data-e2e="comment-level-1"] → 评论文本
│               ├── TUXText → 点赞数 (如 "207")
│               ├── span → 时间 (如 "1-14")
│               └── DivReplyContainer
│                   └── button.TUXButton → "View 7 replies"
```

## API 拦截

### 目标端点
- `/api/comment/list/` — 顶级评论列表
- `/api/comment/list/reply/` — 回复列表

### 关键参数
- `aweme_id`: 视频 ID
- `count`: 每页数量 (20)
- `cursor`: 分页游标

### 响应结构
```json
{
  "comments": [...],
  "cursor": 20,
  "has_more": 1,
  "total": 560
}
```

### 拦截方式（优化版）
通过 Content Script 使用 `chrome.scripting` 在 MAIN world 注入 `interceptor.js`：
- 使用防重入标记 `window.__tceInterceptor`
- 覆写 `window.fetch`，匹配 `/api/comment/list`
- 使用 `resp.clone()` 避免消费原始响应
- 通过 `CustomEvent('tce_api_data')` 传数据（比 postMessage 更安全）
- Content Script 监听 CustomEvent 并转发到 Background

## Popup UI（不变）

```
┌──────────────────────────────┐
│  TikTok 评论导出              │
│  ─────────────────────────── │
│  📹 当前视频:                 │
│  @hubcap2024 · 560 条评论     │
│  ─────────────────────────── │
│  已采集: 0 / 560             │
│  ████░░░░░░░░░░░  0%         │
│  ┌──────────────────────┐    │
│  │   🔍 开始采集评论      │    │
│  └──────────────────────┘    │
│  ┌──────────┐ ┌──────────┐  │
│  │ 📥 导出CSV │ │ 📋 复制全部│  │
│  └──────────┘ └──────────┘  │
│  ─────────────────────────── │
│  ⚙ 包含回复评论: ✅           │
└──────────────────────────────┘
```

## CSV 导出字段

| CSV 列名 | 数据来源 | 说明 |
|---------|---------|------|
| `comment_id` | `cid` | 评论唯一 ID |
| `username` | `user.unique_id` | 用户名 |
| `nickname` | `user.nickname` | 昵称 |
| `comment` | `text` | 评论内容 |
| `likes` | `digg_count` | 点赞数 |
| `replies` | `reply_comment_total` | 回复数 |
| `time` | `create_time` | 格式化时间 |
| `is_reply` | 由 `reply_id` 判断 | 是否为回复 |
| `reply_to` | 父评论用户名 | 回复对象 |
| `creator_liked` | `is_author_digged` | 创作者是否点赞 |

## 文件结构（优化版）

```
54_TiktokVideoCommentCollector/
├── src/
│   ├── background/
│   │   └── service-worker.js      # Service Worker（状态管理、数据持久化）
│   ├── content/
│   │   ├── content-script.js      # 内容脚本（注入、滚动、通信）
│   │   └── interceptor.js         # 页面级 fetch 拦截器（web_accessible）
│   ├── popup/
│   │   ├── popup.html             # 弹窗界面
│   │   ├── popup.js               # 弹窗逻辑
│   │   └── popup.css              # 弹窗样式
│   └── utils/
│       ├── csv-exporter.js        # CSV 生成与下载
│       ├── text-formatter.js      # 文本格式化（复制用）
│       ├── comment-parser.js      # API 响应解析、数据标准化
│       └── constants.js           # 常量定义（API 路径、消息类型）
├── icons/
│   ├── icon-16.png
│   ├── icon-48.png
│   └── icon-128.png
├── manifest.json                  # Chrome 扩展配置 (Manifest V3)
├── CLAUDE.md                      # 项目指令
├── .claude/                       # Lessons 系统
└── docs/
    └── plans/
```

## Manifest V3 权限（优化版）

```json
{
  "manifest_version": 3,
  "permissions": ["activeTab", "storage", "scripting"],
  "host_permissions": ["*://*.tiktok.com/*"],
  "background": { "service_worker": "src/background/service-worker.js" },
  "action": { "default_popup": "src/popup/popup.html" },
  "content_scripts": [{
    "matches": ["*://*.tiktok.com/*"],
    "js": ["src/content/content-script.js"],
    "run_at": "document_start"
  }],
  "web_accessible_resources": [{
    "resources": ["src/content/interceptor.js"],
    "matches": ["*://*.tiktok.com/*"]
  }]
}
```

## 异常处理（优化版）

| 场景 | 处理方式 |
|------|---------|
| 非 TikTok 视频页面 | Popup 显示 "请在 TikTok 视频页面使用" |
| 评论区未加载/Tab 未切换 | 检测 TUXTabBar 状态，自动切换到 Comments Tab |
| API 拦截失败（CSP/注入问题） | 降级提示，建议刷新页面 |
| 自动滚动卡住 | 5 秒无新数据自动停止，保留已采集数据 |
| Service Worker 被终止 | 所有状态存 storage.session，重启后自动恢复 |
| 评论含特殊字符/emoji | CSV 使用 BOM + UTF-8 编码，双引号包裹转义 |
| 网络中断 | 保留已采集数据，可随时导出已有内容 |

## 自动滚动策略（优化版）

- 目标元素：`DivCommentMain`（`overflowY: scroll` 的容器）
- 滚动方式：`element.scrollTop = element.scrollHeight`
- 节奏控制：等待拦截器确认收到 API 响应后再滚动下一页
- 间隔：800ms-1200ms 随机化，模拟人类行为
- 停止条件：`has_more === 0` 或连续 3 次滚动无新数据
- 回复加载：自动点击 "View X replies" 按钮展开回复
